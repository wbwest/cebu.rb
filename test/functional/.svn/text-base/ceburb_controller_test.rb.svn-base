require File.dirname(__FILE__) + '/../test_helper'
require 'ceburb_controller'

# Re-raise errors caught by the controller.
class CeburbController; def rescue_action(e) raise e end; end

class CeburbControllerTest < Test::Unit::TestCase
  def setup
    @controller = CeburbController.new
    @request    = ActionController::TestRequest.new
    @response   = ActionController::TestResponse.new
  end

  # Replace this with your real tests.
  def test_truth
    assert true
  end
end
